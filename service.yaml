apiVersion: v1
kind: Service
metadata:
  name: rate-my-guinness-service
spec:
  type: LoadBalancer
  selector:
    app: rate-my-guinness
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000